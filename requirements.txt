# Core runtime dependencies for SemVulGuard.
#
# This file mirrors the [project].dependencies in pyproject.toml and is enough
# to run the full pipeline EXCEPT the neural ranker/encoder training (which needs
# requirements-models.txt) and ROC-AUC/PR-AUC eval metrics (requirements-eval.txt).
#
# Install with:  pip install -r requirements.txt
# (or, equivalently, the package itself:  pip install -e .)

pydantic>=2,<3
pyyaml>=6
requests>=2.28
